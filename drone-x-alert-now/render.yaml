services:
  - type: web
    name: dronex-python-ai
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python disaster_detection_pipeline.py
    healthCheckPath: /
